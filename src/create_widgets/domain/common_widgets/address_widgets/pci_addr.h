#ifndef PCI_ADDR_H
#define PCI_ADDR_H

#include "_addr.h"

class PciAddr : public _Addr
{
    Q_OBJECT
public:
    explicit PciAddr(QWidget *parent = 0);

signals:

public slots:

};

#endif // PCI_ADDR_H
